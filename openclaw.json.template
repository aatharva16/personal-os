// openclaw.json — Personal AI OS Gateway Configuration
// Template file: deploy.sh runs `envsubst` to produce ~/.openclaw/openclaw.json
// DO NOT commit the rendered file — only commit this template.
// See .env.example for all required environment variables.

{
  // ── Model Providers ────────────────────────────────────────────────────────
  // All inference goes through OpenRouter.
  //
  // Agent messages use openrouter/auto — OpenRouter's server-side intelligent
  // router (powered by NotDiamond). It picks the most cost-effective model per
  // prompt automatically. Configure model restrictions (e.g. Anthropic-only,
  // no Opus) in the OpenRouter Plugins dashboard as account-wide defaults.
  //
  // Heartbeats are pinned to a hardcoded free model so they never consume the
  // auto-router's budget.
  "models": {
    "mode": "merge",
    "providers": {
      "openrouter": {
        "baseUrl": "https://openrouter.ai/api/v1",
        "apiKey": "${OPENROUTER_API_KEY}",
        "api": "openai-completions",
        "models": [
          {
            // openrouter/auto — server-side intelligent routing.
            // Picks the most cost-effective model per prompt automatically.
            "id": "auto",
            "name": "OpenRouter Auto (intelligent routing)",
            "input": ["text"],
            "contextWindow": 200000,
            "maxTokens": 8096
          },
          {
            // Free tier model used exclusively for heartbeats — zero cost.
            // Set HEARTBEAT_MODEL_ID in .env (bare OpenRouter model ID, no prefix).
            "id": "${HEARTBEAT_MODEL_ID}",
            "name": "Heartbeat model (free)",
            "input": ["text"],
            "contextWindow": 10000000,
            "maxTokens": 8096
          }
        ]
      }
    }
  },

  // ── Agent Definitions ──────────────────────────────────────────────────────
  "agents": {
    "defaults": {
      "model": {
        // openrouter/auto handles cheap-vs-premium routing server-side.
        "primary": "openrouter/auto",
        "fallbacks": []
      },
      "heartbeat": {
        // Native per-agent heartbeat is disabled — it has a known multi-agent
        // bug where all agents fire on the main agent's cycle. Use the systemd
        // timer (personal-os-heartbeat.timer) + HEARTBEAT.md files instead.
        "enabled": false
      },
      "compaction": {
        "mode": "default",
        "memoryFlush": {
          "enabled": true,
          "softThresholdTokens": 8000,
          "prompt": "Extract key facts, decisions, open items and preferences into memory/YYYY-MM-DD.md. Bullet points only. Omit small talk.",
          "systemPrompt": "Memory curator. Extract only what is worth remembering long-term."
        }
      },
      "contextPruning": {
        "mode": "cache-ttl",
        "ttl": "6h",
        "keepLastAssistants": 3
      },
      "bootstrapMaxChars": 3000,
      "bootstrapTotalMaxChars": 15000,
      "maxConcurrent": 2,
      "subagents": { "maxConcurrent": 4 }
    },

    "list": [
      {
        // Chief of Staff — primary entry point; routes to specialist agents.
        "id": "chief",
        "name": "Chief of Staff",
        "workspace": "${OPENCLAW_WORKSPACE_ROOT}/workspace-chief"
      },
      {
        // News — daily briefings, current events, tech/markets/geopolitics.
        "id": "news",
        "name": "News",
        "workspace": "${OPENCLAW_WORKSPACE_ROOT}/workspace-news"
      }
    ]
  },

  // ── Channels ───────────────────────────────────────────────────────────────
  "channels": {
    // ── Telegram ─────────────────────────────────────────────────────────────
    // One dedicated bot per agent. Create each bot with @BotFather and set the
    // corresponding token in .env. Only TELEGRAM_ALLOWED_USER_ID can DM any bot.
    "telegram": {
      "enabled": true,
      "bots": [
        { "id": "chief-bot", "botToken": "${TELEGRAM_BOT_TOKEN_CHIEF}" },
        { "id": "news-bot",  "botToken": "${TELEGRAM_BOT_TOKEN_NEWS}" }
      ],
      "dmPolicy": "allowlist",
      "allowFrom": ["${TELEGRAM_ALLOWED_USER_ID}"],
      "streaming": "partial"
    },

    // ── WebChat (Control UI) ──────────────────────────────────────────────────
    // Browser-based control panel. Accessible via token auth.
    // Local:      http://localhost:${WEBCHAT_PORT}
    // Production: http://<tailscale-ip>:${WEBCHAT_PORT}  (Tailscale VPN required)
    "webchat": {
      "enabled": true,
      "token": "${WEBCHAT_TOKEN}",
      "port": "${WEBCHAT_PORT}"
    }
  },

  // ── Agent Bindings ─────────────────────────────────────────────────────────
  // Each Telegram bot DM is routed to its dedicated agent.
  // WebChat sessions are also handled here (default: chief).
  "bindings": [
    {
      "agentId": "chief",
      "match": { "channel": "telegram", "botId": "chief-bot" }
    },
    {
      "agentId": "news",
      "match": { "channel": "telegram", "botId": "news-bot" }
    },
    {
      // WebChat UI defaults to the Chief of Staff.
      "agentId": "chief",
      "match": { "channel": "webchat" }
    }
  ],

  // ── Gateway ────────────────────────────────────────────────────────────────
  // The main gateway port stays loopback (internal only).
  // WebChat binds on its own port (see channels.webchat above).
  "gateway": {
    "port": 18789,
    "mode": "local",
    "bind": "loopback"
  },

  // ── Logging ────────────────────────────────────────────────────────────────
  "logging": {
    "redactSensitive": "tools"
  }
}
