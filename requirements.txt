# Python dependencies for Personal AI OS scripts.
#
# Installed by deploy.sh:
#   sudo apt-get install -y python3-requests
#   sudo pip3 install --break-system-packages mcp
#
# The plain `pip3 install -r requirements.txt` form does NOT work on
# Debian/Ubuntu 22.04+ due to PEP 668 (externally-managed-environment).
# Use the deploy.sh install step, or run manually with --break-system-packages.
mcp
requests
